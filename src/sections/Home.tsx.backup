import { useState, useEffect } from 'react';
import { ArrowRight, Play, ChevronLeft, ChevronRight } from 'lucide-react';

export default function Home() {
  const [currentWord, setCurrentWord] = useState(0);
  const [currentTestimonial, setCurrentTestimonial] = useState(0);

  const powerWords = ['MEISTERSCHAFT', 'KLARHEIT', 'MOMENTUM'];

  const trustedLogos = [
    'Fortune 500', 'Goldman Sachs', 'McKinsey', 'Google', 'Microsoft', 'Tesla'
  ];

  const pillars = [
    {
      title: 'VERSTAND',
      description: 'Neuronale Neuverdrahtung für kognitive Spitzenleistung und Entscheidungsklarheit'
    },
    {
      title: 'EMOTION',
      description: 'Beherrschung psychologischer Zustände für nachhaltige Höchstleistung'
    },
    {
      title: 'MOMENTUM',
      description: 'Systemtechnik für exponentielles Wachstum und Durchbruchergebnisse'
    }
  ];

  const keynotes = [
    {
      title: 'Signal Thinking',
      image: 'https://images.pexels.com/photos/3184291/pexels-photo-3184291.jpeg?auto=compress&cs=tinysrgb&w=800',
      audience: 'C-Level Führungskräfte'
    },
    {
      title: 'Neuronale Führung',
      image: 'https://images.pexels.com/photos/2774556/pexels-photo-2774556.jpeg?auto=compress&cs=tinysrgb&w=800',
      audience: 'Leadership Teams'
    },
    {
      title: 'Durchbruch unter Druck',
      image: 'https://images.pexels.com/photos/3184357/pexels-photo-3184357.jpeg?auto=compress&cs=tinysrgb&w=800',
      audience: 'High Performer'
    }
  ];

  const testimonials = [
    {
      quote: "Anatolys Framework hat unseren gesamten Führungsansatz transformiert. Die Ergebnisse waren sofort messbar.",
      name: 'Sarah Chen',
      role: 'CEO, TechVentures'
    },
    {
      quote: "Die wirkungsvollste Keynote, die ich in 20 Jahren erlebt habe. Seine Methodik ist revolutionär.",
      name: 'Michael Rodriguez',
      role: 'VP Strategie, Global Corp'
    },
    {
      quote: "Von skeptisch zu überzeugt. Der neurowissenschaftliche Ansatz liefert echte, dauerhafte Veränderung.",
      name: 'Dr. Emily Watson',
      role: 'Chief Medical Officer'
    }
  ];

  useEffect(() => {
    const interval = setInterval(() => {
      setCurrentWord((prev) => (prev + 1) % powerWords.length);
    }, 2000);
    return () => clearInterval(interval);
  }, []);

  return (
    <div className="bg-black">
      <section className="relative h-screen flex items-center justify-center overflow-hidden">
        <div className="absolute inset-0 bg-gradient-to-br from-black via-gray-900 to-black">
          <img
            src="https://images.pexels.com/photos/3184291/pexels-photo-3184291.jpeg?auto=compress&cs=tinysrgb&w=1920"
            alt="Anatoly Mook"
            className="w-full h-full object-cover opacity-30 mix-blend-luminosity"
          />
        </div>

        <div className="relative z-10 text-center px-6 max-w-5xl">
          <div className="mb-8 h-32 flex items-center justify-center">
            <h1 className="text-7xl lg:text-9xl font-extralight tracking-tight text-white transition-all duration-700">
              {powerWords[currentWord]}
            </h1>
          </div>

          <p className="text-xl lg:text-2xl font-light text-white/80 mb-12 tracking-wide">
            Durchbruch-Performance durch Neurowissenschaft und Momentum-Systeme
          </p>

          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            <button className="px-8 py-4 bg-yellow-500 text-black font-light tracking-wider hover:bg-yellow-400 transition-all duration-300 flex items-center justify-center group">
              VORTRAG BUCHEN
              <ArrowRight className="ml-2 group-hover:translate-x-1 transition-transform" size={20} />
            </button>
            <button className="px-8 py-4 border border-white/30 text-white font-light tracking-wider hover:bg-white/10 transition-all duration-300 flex items-center justify-center">
              <Play className="mr-2" size={20} />
              SHOWREEL ANSEHEN
            </button>
          </div>
        </div>

        <div className="absolute bottom-12 left-1/2 -translate-x-1/2 animate-bounce">
          <div className="w-6 h-10 border-2 border-white/30 rounded-full flex justify-center pt-2">
            <div className="w-1 h-3 bg-white/50 rounded-full"></div>
          </div>
        </div>
      </section>

      <section className="py-20 border-t border-white/10">
        <div className="max-w-7xl mx-auto px-6 lg:px-12">
          <p className="text-center text-white/40 text-sm font-light tracking-widest mb-12">
            VERTRAUT VON BRANCHENFÜHRERN
          </p>
          <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-8">
            {trustedLogos.map((logo, idx) => (
              <div
                key={idx}
                className="flex items-center justify-center text-white/30 hover:text-white/60 transition-colors text-sm font-light tracking-wider"
              >
                {logo}
              </div>
            ))}
          </div>
        </div>
      </section>

      <section className="py-32 bg-gradient-to-b from-black to-gray-900">
        <div className="max-w-7xl mx-auto px-6 lg:px-12">
          <div className="text-center mb-20">
            <h2 className="text-5xl lg:text-6xl font-extralight text-white mb-6 tracking-tight">
              DIE METHODE
            </h2>
            <p className="text-xl text-white/60 font-light max-w-3xl mx-auto">
              Ein systematischer Ansatz zur Freisetzung exponentieller Performance durch drei Kernsäulen
            </p>
          </div>

          <div className="grid md:grid-cols-3 gap-8">
            {pillars.map((pillar, idx) => (
              <div
                key={idx}
                className="group relative bg-white/5 backdrop-blur-sm border border-white/10 p-12 hover:bg-white/10 hover:border-yellow-500/50 transition-all duration-500"
              >
                <div className="absolute top-8 left-8 text-7xl font-extralight text-yellow-500/20 group-hover:text-yellow-500/40 transition-colors">
                  {idx + 1}
                </div>
                <h3 className="relative text-3xl font-light text-white mb-6 tracking-wider">
                  {pillar.title}
                </h3>
                <p className="relative text-white/60 font-light leading-relaxed">
                  {pillar.description}
                </p>
              </div>
            ))}
          </div>
        </div>
      </section>

      <section className="py-32 bg-black">
        <div className="max-w-7xl mx-auto px-6 lg:px-12">
          <div className="text-center mb-20">
            <h2 className="text-5xl lg:text-6xl font-extralight text-white mb-6 tracking-tight">
              SIGNATURE VORTRÄGE
            </h2>
            <p className="text-xl text-white/60 font-light">
              Transformieren Sie Ihre Organisation mit Weltklasse-Präsentationen
            </p>
          </div>

          <div className="grid md:grid-cols-3 gap-8">
            {keynotes.map((keynote, idx) => (
              <div
                key={idx}
                className="group relative overflow-hidden cursor-pointer"
              >
                <div className="aspect-[4/5] relative overflow-hidden">
                  <img
                    src={keynote.image}
                    alt={keynote.title}
                    className="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110"
                  />
                  <div className="absolute inset-0 bg-gradient-to-t from-black via-black/50 to-transparent opacity-80"></div>
                </div>
                <div className="absolute bottom-0 left-0 right-0 p-8">
                  <p className="text-yellow-500 text-sm font-light tracking-wider mb-2">
                    {keynote.audience}
                  </p>
                  <h3 className="text-3xl font-light text-white mb-4">
                    {keynote.title}
                  </h3>
                  <button className="text-white/80 hover:text-yellow-500 transition-colors flex items-center text-sm font-light tracking-wider">
                    MEHR ERFAHREN
                    <ArrowRight className="ml-2" size={16} />
                  </button>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      <section className="py-32 bg-gradient-to-b from-gray-900 to-black relative overflow-hidden">
        <div className="absolute inset-0 opacity-5">
          <div className="absolute top-1/4 left-1/4 w-96 h-96 bg-yellow-500 rounded-full blur-3xl"></div>
          <div className="absolute bottom-1/4 right-1/4 w-96 h-96 bg-yellow-500 rounded-full blur-3xl"></div>
        </div>

        <div className="relative max-w-5xl mx-auto px-6 lg:px-12">
          <div className="text-center mb-20">
            <h2 className="text-5xl lg:text-6xl font-extralight text-white mb-6 tracking-tight">
              ERFOLGSGESCHICHTEN
            </h2>
          </div>

          <div className="relative">
            <div className="bg-white/5 backdrop-blur-sm border border-white/10 p-12 lg:p-16">
              <p className="text-2xl lg:text-3xl font-light text-white mb-8 leading-relaxed">
                "{testimonials[currentTestimonial].quote}"
              </p>
              <div>
                <p className="text-white font-light text-lg">
                  {testimonials[currentTestimonial].name}
                </p>
                <p className="text-white/60 font-light">
                  {testimonials[currentTestimonial].role}
                </p>
              </div>
            </div>

            <div className="flex justify-center gap-4 mt-8">
              <button
                onClick={() => setCurrentTestimonial((prev) => (prev - 1 + testimonials.length) % testimonials.length)}
                className="w-12 h-12 rounded-full bg-white/5 hover:bg-white/10 border border-white/10 flex items-center justify-center text-white transition-all"
              >
                <ChevronLeft size={20} />
              </button>
              <button
                onClick={() => setCurrentTestimonial((prev) => (prev + 1) % testimonials.length)}
                className="w-12 h-12 rounded-full bg-white/5 hover:bg-white/10 border border-white/10 flex items-center justify-center text-white transition-all"
              >
                <ChevronRight size={20} />
              </button>
            </div>
          </div>
        </div>
      </section>

      <section className="py-32 bg-yellow-500 relative overflow-hidden">
        <div className="max-w-4xl mx-auto px-6 lg:px-12 text-center">
          <h2 className="text-5xl lg:text-6xl font-extralight text-black mb-6 tracking-tight">
            STARTEN SIE IHRE TRANSFORMATION
          </h2>
          <p className="text-xl text-black/80 font-light mb-12">
            Laden Sie den Momentum-Blueprint herunter und entdecken Sie das 7-Stufen-Framework von Top-Performern weltweit
          </p>
          <div className="flex flex-col sm:flex-row gap-4 justify-center items-center">
            <input
              type="email"
              placeholder="Ihre E-Mail-Adresse"
              className="w-full sm:w-96 px-6 py-4 bg-white/90 text-black placeholder-black/40 font-light focus:outline-none focus:ring-2 focus:ring-black"
            />
            <button className="px-8 py-4 bg-black text-white font-light tracking-wider hover:bg-gray-900 transition-all duration-300 flex items-center justify-center whitespace-nowrap">
              BLUEPRINT KOSTENLOS
              <ArrowRight className="ml-2" size={20} />
            </button>
          </div>
        </div>
      </section>
    </div>
  );
}
